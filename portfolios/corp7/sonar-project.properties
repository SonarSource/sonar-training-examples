# Required metadata
sonar.projectKey=BANK-CORP-TAXOPTIMIZATION
sonar.projectName=Corporate Banking - Tax Optimization
sonar.projectVersion=13.6

# Comma-separated paths to directories with sources (required)
sonar.sources=src
sonar.java.binaries=/tmp

# Encoding of the source files
sonar.sourceEncoding=UTF-8

